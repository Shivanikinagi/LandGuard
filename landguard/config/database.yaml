# Database configuration

postgresql:
  host: localhost
  port: 5432
  database: landguard
  user: landguard_user
  password: LandGuard2024!
  pool_size: 10
  max_overflow: 20
  echo: false

mongodb:
  host: localhost
  port: 27017
  database: landguard_docs
  user: null  # Set to null instead of empty string
  password: null  # Set to null instead of empty string
  
# Connection pooling
pool:
  min_size: 5
  max_size: 20
  timeout: 30

# Migration settings
migrations:
  directory: database/migrations/versions
  auto_generate: true